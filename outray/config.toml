# OutRay tunnels for Naivolt development
# Run: npx outray start
# Keep this running, then start backend (port 5000) and Expo (port 8081) locally.

[tunnel.metro]
protocol = "http"
local_port = 8081
local_host = "localhost"

[tunnel.api]
protocol = "http"
local_port = 5000
local_host = "localhost"
